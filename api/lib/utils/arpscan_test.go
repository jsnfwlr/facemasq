package utils

import (
	"testing"
)
/*
func init() {
	flag.VisitAll(func(flagg *flag.Flag) {
		fmt.Println(flagg.Name)
	})
	for i := range os.Args {
		fmt.Println(os.Args[i])
	}
	if flag.Lookup("test.v") == nil {
		fmt.Println("no flag 'test.v'")
	} else {
		fmt.Println("flag 'test.v' is present")
	}
	if strings.HasSuffix(os.Args[0], ".test") {
		fmt.Println("arg exists with '.test' in name")
	}

}
*/
func TestFlags(t *testing.T) {
	t.Error("Nope")
}
